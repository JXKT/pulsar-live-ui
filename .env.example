# ------------------------------------------------------------------------
# PULSAR LIVE UI - CONFIGURATION
# ------------------------------------------------------------------------

# The binary protocol URL for the Pulsar Broker (TCP)
# Local default: pulsar://localhost:6650
PULSAR_BROKER_URL=pulsar://<your-broker-host>:<port>

# The HTTP URL for the Pulsar Admin Interface
# Local default: http://localhost:8080
PULSAR_ADMIN_URL=http://<your-admin-host>:<port>

# Authentication Token (JWT)
# Required if your cluster has authentication enabled.
# Leave blank or comment out if using a standalone local cluster without auth.
PULSAR_TOKEN=<your-long-jwt-token-string-here>